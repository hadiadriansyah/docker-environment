# Ignore all contents inside the projects/ folder
projects/*

# Except .gitkeep so the folder itself remains tracked
!projects/.gitkeep

# Allow tracking of everything inside the example/ folder
!projects/example/
!projects/example/**